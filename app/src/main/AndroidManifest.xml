<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature
        android:name="android.hardware.sensor.proximity"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mumdroid">
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Mumdroid"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.ConnectionActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Mumdroid"
            android:windowSoftInputMode="adjustResize">
            <!-- Notification tap target. The service builds this action into an
                 activity PendingIntent instead of referencing the class, keeping
                 the service free of any ui dependency. -->
            <intent-filter>
                <action android:name="dev.woms.mumdroid.action.OPEN_SESSION" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.SettingsActivity"
            android:exported="false"
            android:label="@string/settings"
            android:theme="@style/Theme.Mumdroid" />

        <activity
            android:name=".ui.activity.AudioSettingsActivity"
            android:exported="false"
            android:label="@string/settings_audio"
            android:theme="@style/Theme.Mumdroid" />

        <activity
            android:name=".ui.activity.NetworkSettingsActivity"
            android:exported="false"
            android:label="@string/settings_network"
            android:theme="@style/Theme.Mumdroid" />

        <activity
            android:name=".ui.activity.IdentitySettingsActivity"
            android:exported="false"
            android:label="@string/settings_identity"
            android:theme="@style/Theme.Mumdroid"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.activity.AppearanceSettingsActivity"
            android:exported="false"
            android:label="@string/settings_appearance"
            android:theme="@style/Theme.Mumdroid" />

        <activity
            android:name=".ui.activity.GeneralSettingsActivity"
            android:exported="false"
            android:label="@string/settings_general"
            android:theme="@style/Theme.Mumdroid" />

        <activity
            android:name=".ui.activity.AboutActivity"
            android:exported="false"
            android:label="@string/settings_about"
            android:theme="@style/Theme.Mumdroid" />

        <service
            android:name=".service.MumbleService"
            android:exported="false"
            android:foregroundServiceType="microphone" />
    </application>

</manifest>
